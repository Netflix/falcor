/**
 * An envelope that wraps a JSON object
 * @typedef {Object} JSONEnvelope
 * @property {JSON} json - A JSON object
 * @property {?Array.<PathSet>} paths - The paths to the values in the JSON object
 * @property {?Array.<Error>} errors - One or more errors that occurred while executing an operation
 * @property {?Array.<PathSet>} invalidated - The paths to invalidate in the {@link Model}
 * @example TODO: Fill out a simple example showing the data structure
 */
